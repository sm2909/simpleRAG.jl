# simpleRAG.jl

A minimal, from-scratch Retrieval-Augmented Generation library in Julia.

Current goals:

- Load and chunk documents
- Store chunks in memory
- Retrieval based on similarity
- Later: add embeddings, vector search, and LLM integration
